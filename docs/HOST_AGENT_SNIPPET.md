# Host agent instruction snippet

Add this line to your host project's agent instructions file (e.g. `.github/instructions/project.instructions.md`):

```
When working with files inside /mods/, follow the workflow in mods/docs/AGENT_GUIDE.md.
```

Adjust `/mods/` if your MODS tool directory uses a different path.
